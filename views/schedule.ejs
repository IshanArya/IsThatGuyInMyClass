<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/css/skeleton.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<div class="container">
    <div class="row">
        <div class="four columns">
            <h4>Schedule for <%= student.name %></h4>
        </div>
    </div>
    <div class="row">
        <div class="four columns">
            <table class="u-full-width">
                <thead>
                    <th>Period</th>
                    <th>Room #</th>
                </thead>
                <tbody>
                    <% for (var i = 0; i < student.classes.length; i++) { %>
                    <tr>
                        <td><%= i + 1 %></td>
                        <td><%= student.classes[i] %></td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>

</body>
</html>